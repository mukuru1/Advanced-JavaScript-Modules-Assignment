import { add, square, capitalize } from './utils/index.js';

const a = 5;
const b = 3;
console.log(`add(${a}, ${b}) = ${add(a, b)}`);
console.log(`square(${b}) = ${square(b)}`);
console.log(`capitalize("hello") = ${capitalize('hello')}`);